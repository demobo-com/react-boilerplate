@import '~scss_mixins';
@import '~scss_vars';

.{{  kebabCase name }} {
  // > xl

  @include privat-respond-to('lg') {
    // <= xl
  }

  @include privat-respond-to('md') {
    // <= lg
  }

  @include privat-respond-to('sm') {
    // <= md
  }

  @include privat-respond-to('xs') {
    // <= sm
  }

  @include privat-respond-to('only-lg') {
    // >= sm + 1  and <= xl
  }

  @include privat-respond-to('only-md') {
    // >= md + 1  and <= lg
  }

  @include privat-respond-to('only-sm') {
    // >= sm + 1  and <= md
  }
}
